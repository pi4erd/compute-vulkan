#version 450 core

layout (binding = 0) buffer NumbersSSBO {
    int numbers[ ];
};

layout (local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

void main() {
    uint index = gl_GlobalInvocationID.x;

    numbers[index] = 1000000000 / numbers[index];
}
